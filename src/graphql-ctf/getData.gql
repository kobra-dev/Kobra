query GetData {
    assetCollection(where: { title_in: ["Favicon"] }) {
        items {
            url
        }
    }
    pageCollection(where: { id: "Index" }, limit: 1) {
        items {
            navbar {
                logo {
                    url
                }
            }
        }
    }
}